class student{
    constructor(id, name, entry){
        this.id = id;
        this.name = name;
        this.entry = entry;
        this.exit = null;
    }
}

class database{
    static let data = [];

    static add(student)

    static exists(name, id)

    static change()
}